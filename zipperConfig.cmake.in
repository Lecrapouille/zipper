@PACKAGE_INIT@

# Zipper CMake Configuration File
# To use this simply use `target_link_libraries` against `zipper::zipper`

include("${CMAKE_CURRENT_LIST_DIR}/zipperTargets.cmake")


# Deprecated variables, do not use!
# These are meant to be used purely for backwards compatibility with a potential
# FindZipper.cmake module. The names are provided as a best effort basis.

get_target_property(ZIPPER_INCLUDE_DIRS zipper::zipper INTERFACE_INCLUDE_DIRECTORIES)
set(ZIPPER_LIBRARIES zipper::zipper)
